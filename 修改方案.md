# Word模板修改方案 (更新版)

**分析日期**: 2026-01-02
**模板文件**: `config/template_final.docx`
**最后更新**: 2026-01-02

---

## 问题概述

经过详细分析 `template_final.docx` 模板文件，发现以下问题导致数据无法正确插入：

| 问题类型 | 数量 | 严重程度 | 状态 |
|---------|------|---------|------|
| 拼写错误 | 15处 | 🔴 严重 | 待修复 |
| 大小写不匹配 | 11处 | 🔴 严重 | 待修复 |
| 循环变量引用错误 | 3处 | 🔴 严重 | ✅ 已修复 |
| 缺失变量 | 2处 | 🟡 中等 | 待修复 |

---

## 一、拼写错误 (最严重问题)

### 1.1 `anyo_ejecicio` 拼写错误 (出现 13 次)

**问题**: 变量名缺少字母 `r`，导致所有年份字段无法渲染

| 错误写法 | 正确写法 | 出现次数 |
|---------|---------|---------|
| `{{ anyo_ejecicio }}` | `{{ anyo_ejercicio }}` | 13 |
| `{{ anyo_ejecicio_ant }}` | `{{ anyo_ejercicio_ant }}` | 2 |
| `{{anyo_ejecicio}}` | `{{ anyo_ejercicio }}` | 若干 |

**影响范围**:
- 封面标题
- 财务数据表格表头
- 多处文档正文

**修复方法**:
```
查找: anyo_ejecicio
替换: anyo_ejercicio
```

---

## 二、大小写不匹配 (严重问题)

docxtpl (Jinja2) 是**大小写敏感**的。模板中使用了首字母大写或驼峰命名，但系统生成的上下文使用全小写。

### 2.1 联系人信息变量

| 模板中的错误写法 | 正确写法 |
|-----------------|---------|
| `{{ Contacto1 }}` | `{{ contacto1 }}` |
| `{{ Contacto2 }}` | `{{ contacto2 }}` |
| `{{ Contacto3 }}` | `{{ contacto3 }}` |
| `{{ Cargo_contacto1 }}` | `{{ cargo_contacto1 }}` |
| `{{ Cargo_contacto2 }}` | `{{ cargo_contacto2 }}` |
| `{{ Cargo_contacto3 }}` | `{{ cargo_contacto3 }}` |

**注意**: `correo_contacto1/2/3` 已经是正确的小写格式 ✓

### 2.2 全局变量

| 模板中的错误写法 | 正确写法 |
|-----------------|---------|
| `{{ Entidad_cliente }}` | `{{ entidad_cliente }}` |
| `{{ Descripción_actividad }}` | `{{ descripcion_actividad }}` |
| `{{ Texto_anexo3 }}` | `{{ texto_anexo3 }}` |

---

## 三、动态表格循环变量引用错误 (✅ 已修复)

### 3.1 SEC2_TABLE_03 - Tipo de Operación Vinculada

**问题说明**: 循环变量必须通过循环变量访问属性

**正确的表格行循环语法** (使用 `{%tr %}` 而不是 `{% %}`):
```jinja2
{%tr for servicio in servicios_vinculados %}
{%tr for entidad in servicio.entidades_vinculadas %}
| {{ loop.index }} | {{ servicio.servicio_vinculado }} | {{ entidad.entidad_vinculada }} | {{ entidad.ingreso_entidad }} |
{%tr endfor %}
{%tr endfor %}
```

**重要**: 在 docxtpl 中，表格行循环必须使用 `{%tr %}` 标签，以确保整行被正确重复。

### 3.2 消除循环中的额外空行

在循环中使用空白控制标记 `{%-` 和 `-%}` 来消除多余的空行:

```jinja2
{%- for servicio in servicios_vinculados -%}
{{ servicio.servicio_vinculado }}
{%- endfor -%}
```

### 3.3 数据结构参考

`servicios_vinculados` 的数据结构如下：
```python
servicios_vinculados = [
    {
        "servicio_vinculado": "Servicios de gestión",
        "entidades_vinculadas": [
            {
                "entidad_vinculada": "Empresa A",
                "ingreso_entidad": 100000,
                "gasto_entidad": 50000
            },
            {
                "entidad_vinculada": "Empresa B",
                "ingreso_entidad": 200000,
                "gasto_entidad": 80000
            }
        ]
    }
]
```

---

## 四、缺失变量

### 4.1 `gasto_entidad` 完全缺失

**问题**: 在 SEC2_TABLE_03 关联交易表格中，只有 `ingreso_entidad` (收入)，缺少 `gasto_entidad` (支出)

**需要添加**:
```jinja2
{{ entidad.gasto_entidad }}
```

### 4.2 `impacto_2` 缺失

**问题**: 风险评估表格 (ANEXO1_TABLE_07) 第2行缺少 `impacto_2` 变量

**需要添加**:
```jinja2
{{ impacto_2 }}
```

---

## 五、代码修复记录 (2026-01-02)

以下代码问题已在应用程序中修复:

### 5.1 变量值输出格式 ✅ 已修复

**问题**: 文本列表变量输出 `{'value': 'PG'}` 而不是 `'PG'`

**修复位置**:
- `ui/streamlit_app/state_store.py` - `get_all_form_data()`
- `ui/api/ui/app.js` - `getAllData()`

**修复说明**: 简单文本列表现在会自动扁平化，仅包含 `value` 键的项会被转换为纯字符串值。

### 5.2 小数精度 ✅ 已修复

**问题**: 财务数据中的变化率小数点过长

**修复位置**: `modules/context_builder.py`

**修复说明**: 所有百分比和变化率计算现在使用 `round_decimal()` 函数四舍五入到2位小数。

### 5.3 西语月份缩写 ✅ 已修复

**问题**: `fecha_fin_fiscal` 变量应使用西语月份缩写

**修复位置**:
- `modules/context_builder.py` - 添加 `SPANISH_MONTH_ABBR` 字典
- `modules/context_builder.py` - 添加 `format_date_short_spanish()` 函数

**月份对照表**:
| 月份 | 缩写 |
|-----|-----|
| 1 | Ene |
| 2 | Feb |
| 3 | Mar |
| 4 | Abr |
| 5 | May |
| 6 | Jun |
| 7 | Jul |
| 8 | Ago |
| 9 | Sep |
| 10 | Oct |
| 11 | Nov |
| 12 | Dic |

### 5.4 颜色格式化 ✅ 已修复

**问题**:
- `cumplimiento_resumen_local_i` 和 `cumplimiento_resumen_mast_i`: "si" 应为绿色，"no" 应为红色
- `impacto_i` 不应有背景色变化

**修复位置**:
- `config/yamls/pt_review/formatting.yaml` - 更新 `compliance_color` 配置
- `modules/renderer_docx.py` - 添加列上下文检测以排除 impacto 列

**颜色配置**:
| 值 | 背景色 |
|---|--------|
| si | #00B050 (绿色) |
| no | #FF0000 (红色) |
| parcial | #FFFF00 (黄色) |

### 5.5 页面分隔符 ✅ 已修复

**问题**: 模板中的 `[PAGE_BREAK]` 标记需要转换为实际的分页符

**修复位置**: `modules/renderer_docx.py` - `_process_page_breaks()`

**使用方法**: 在模板中需要分页的位置添加 `[PAGE_BREAK]` 标记

### 5.6 元数据导出 ✅ 已修复

**问题**: 导出的JSON元数据重新导入时无法完全恢复所有数据

**修复位置**: `ui/streamlit_app/app.py` - `export_form_data()` 和 `load_json_data()`

**修复说明**:
- 导出格式升级到 v2.0，使用 `_list_items` 单独存储列表数据
- 导入功能支持 v1.0 和 v2.0 两种格式

---

## 六、表格自动编号 ✅ 已实现

### 6.1 使用方法

在模板中使用 `tabla_num.next()` 获取下一个表格编号:

```jinja2
Tabla {{ tabla_num.next() }}. Identificación de las operaciones intragrupo en el Ejercicio {{ anyo_ejercicio }}
```

**示例输出**:
- Tabla 1. Identificación de las operaciones...
- Tabla 2. Datos Financieros...
- Tabla 3. Operaciones Vinculadas...

### 6.2 代码实现

**位置**: `modules/context_builder.py` - `TableCounter` 类

```python
class TableCounter:
    def next(self) -> int:  # 获取下一个编号
    def current(self) -> int:  # 获取当前编号（不递增）
    def reset(self, value: int = 0) -> None:  # 重置计数器
```

---

## 七、目录自动更新 ✅ 已实现

### 7.1 功能说明

文档生成时会设置 `updateFields` 属性，使 Word 在打开文档时自动提示更新目录。

**修复位置**: `modules/renderer_docx.py` - `_set_update_fields_on_open()`

### 7.2 使用说明

1. 在模板的第二页设置目录字段 (TOC)
2. 生成文档后，用 Word 打开
3. Word 会提示更新字段，点击"是"即可更新目录

---

## 八、模板语法指南

### 8.1 表格行循环

**正确语法** (使用 `{%tr %}`):
```jinja2
{%tr for item in items %}
| {{ item.field1 }} | {{ item.field2 }} |
{%tr endfor %}
```

**错误语法** (不要使用 `{% %}`):
```jinja2
{% for item in items %}  <!-- 错误! -->
| {{ item.field1 }} | {{ item.field2 }} |
{% endfor %}
```

### 8.2 空白控制

消除循环产生的多余空行:
```jinja2
{%- for item in items -%}
{{ item.value }}
{%- endfor -%}
```

### 8.3 条件渲染

```jinja2
{% if master_file == 1 %}
此内容仅在 master_file = 1 时显示
{% endif %}
```

---

## 九、完整修复清单

### 9.1 模板批量替换操作 (按优先级排序)

| # | 查找 | 替换为 | 次数 |
|---|------|--------|------|
| 1 | `anyo_ejecicio` | `anyo_ejercicio` | 15 |
| 2 | `Entidad_cliente` | `entidad_cliente` | 2 |
| 3 | `Contacto1` | `contacto1` | 1 |
| 4 | `Contacto2` | `contacto2` | 1 |
| 5 | `Contacto3` | `contacto3` | 1 |
| 6 | `Cargo_contacto1` | `cargo_contacto1` | 1 |
| 7 | `Cargo_contacto2` | `cargo_contacto2` | 1 |
| 8 | `Cargo_contacto3` | `cargo_contacto3` | 1 |
| 9 | `Descripción_actividad` | `descripcion_actividad` | 1 |
| 10 | `Texto_anexo3` | `texto_anexo3` | 1 |

### 9.2 模板手动修复操作

1. **SEC2_TABLE_03 循环变量修复**:
   - 找到 `{% for servicio in servicios_vinculados %}` 区域
   - 改为 `{%tr for servicio in servicios_vinculados %}`
   - 将 `{{ servicio_vinculado }}` 改为 `{{ servicio.servicio_vinculado }}`
   - 将 `{{ entidad_vinculada }}` 改为 `{{ entidad.entidad_vinculada }}`
   - 将 `{{ ingreso_entidad }}` 改为 `{{ entidad.ingreso_entidad }}`
   - 添加 `{{ entidad.gasto_entidad }}` 到支出列

2. **添加缺失的 impacto_2**:
   - 在风险表格第2行添加 `{{ impacto_2 }}`

3. **表格编号更新**:
   - 将所有 "Tabla 1.", "Tabla 2." 等静态编号改为 `Tabla {{ tabla_num.next() }}.`

---

## 十、修复脚本 (Python)

```python
from docx import Document
import re

def fix_template(input_path, output_path):
    """修复模板变量"""
    doc = Document(input_path)

    # 替换规则
    replacements = {
        'anyo_ejecicio_ant': 'anyo_ejercicio_ant',  # 先替换长的
        'anyo_ejecicio': 'anyo_ejercicio',
        'Entidad_cliente': 'entidad_cliente',
        'Contacto1': 'contacto1',
        'Contacto2': 'contacto2',
        'Contacto3': 'contacto3',
        'Cargo_contacto1': 'cargo_contacto1',
        'Cargo_contacto2': 'cargo_contacto2',
        'Cargo_contacto3': 'cargo_contacto3',
        'Descripción_actividad': 'descripcion_actividad',
        'Texto_anexo3': 'texto_anexo3',
        # 循环变量修复
        '{{ servicio_vinculado }}': '{{ servicio.servicio_vinculado }}',
        '{{ entidad_vinculada }}': '{{ entidad.entidad_vinculada }}',
        '{{ ingreso_entidad }}': '{{ entidad.ingreso_entidad }}',
        # 表格行循环语法修复
        '{% for servicio in servicios_vinculados %}': '{%tr for servicio in servicios_vinculados %}',
        '{% endfor %}': '{%tr endfor %}',
    }

    def replace_in_runs(paragraph):
        """替换段落中的文本"""
        full_text = ''.join(run.text for run in paragraph.runs)
        modified = full_text
        for old, new in replacements.items():
            modified = modified.replace(old, new)
        if modified != full_text:
            # 清空所有runs并在第一个run中设置新文本
            for i, run in enumerate(paragraph.runs):
                if i == 0:
                    run.text = modified
                else:
                    run.text = ''

    # 处理段落
    for para in doc.paragraphs:
        replace_in_runs(para)

    # 处理表格
    for table in doc.tables:
        for row in table.rows:
            for cell in row.cells:
                for para in cell.paragraphs:
                    replace_in_runs(para)

    doc.save(output_path)
    print(f"已保存修复后的模板: {output_path}")

# 使用方法
# fix_template('config/template_final.docx', 'config/template_final_fixed.docx')
```

---

## 十一、验证检查

修复后，请运行以下检查确保所有变量正确：

```bash
# 验证脚本
python3 -c "
from docx import Document
doc = Document('config/template_final.docx')
errors = []
error_patterns = [
    'anyo_ejecicio', 'Contacto1', 'Contacto2', 'Contacto3',
    'Cargo_contacto', 'Entidad_cliente', 'Descripción_actividad',
    'Texto_anexo3', '{{ servicio_vinculado }}', '{{ entidad_vinculada }}',
    '{{ ingreso_entidad }}'
]

for table in doc.tables:
    for row in table.rows:
        for cell in row.cells:
            text = cell.text
            for pattern in error_patterns:
                if pattern in text:
                    errors.append(f'发现错误: {pattern}')

for para in doc.paragraphs:
    for pattern in error_patterns:
        if pattern in para.text:
            errors.append(f'发现错误: {pattern}')

if errors:
    print('\\n'.join(errors))
else:
    print('✓ 所有检查通过')
"
```

---

## 十二、重要提示

1. **修改前备份**: 在修改 `template_final.docx` 前，请先创建备份
2. **全小写规则**: 所有变量名必须使用小写字母和下划线
3. **循环变量前缀**: 在 for 循环内部访问属性时，必须使用循环变量作为前缀
4. **表格行循环**: 使用 `{%tr %}` 而不是 `{% %}` 进行表格行循环
5. **测试验证**: 修改后请使用实际数据测试文档生成

---

*最后更新: 2026-01-02*
